{
  "name": "wpnd-playground",
  "private": true,
  "scripts": {
    "wpnd": "../src/bin/index.js",
    "start": "yarn wpnd start",
    "destroy": "yarn wpnd destroy",
    "shell:wordpress": "yarn wpnd shell",
    "shell:db": "yarn wpnd shell -s db"
  }
}
